<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.ondraft.beer.MainActivity">
    <item
        android:id="@+id/search_by_name"
        android:orderInCategory="100"
        android:title="@string/search_by_name"
        app:showAsAction="never" />

    <item
        android:id="@+id/search_by_state"
        android:orderInCategory="100"
        android:title="@string/search_by_state"
        app:showAsAction="never" />

    <item
        android:id="@+id/search_by_city"
        android:orderInCategory="100"
        android:title="@string/search_by_city"
        app:showAsAction="never" />
</menu>
